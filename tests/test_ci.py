def test_github_workflow():
    """This function acts as a dummy pytest function which always returns True. In case no other pytest functions
    are present, this will ensure GitHub testing workflow does not fail due to lack of pytest functions."""
    print("Dummy pytest successfully executed.")
    assert True
